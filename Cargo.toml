[package]
name = "herdr-open-in-zed"
version = "0.1.0"
edition = "2024"
license = "MIT"
rust-version = "1.85"
description = "herdr plugin that opens the current workspace in the Zed editor"

[dependencies]
serde_json = "1"
